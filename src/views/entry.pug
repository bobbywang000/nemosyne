doctype html
html
  head
    meta(charset='utf-8')
    style
        include css/entry.css
    title date
  body
    h1= date
    h2 Written #{writeDate}
    h3 In ranges: #{JSON.stringify(parentRanges)}

    | !{ content }
